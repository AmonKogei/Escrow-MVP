# Local SQLite environment for quick testing without Docker
# Copy to `.env.sqlite.local` or set in your environment when running dev/test scripts
DATABASE_URL="file:./dev.db"
NODE_ENV=development
SESSION_SECRET=change_this_to_a_secure_random_value
