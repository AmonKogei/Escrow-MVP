# Example environment variables
# Copy this file to `.env` and fill in the values before running locally.

# Postgres database (used by Prisma). If you don't have Postgres locally, you can
# use the included SQLite test DB by creating a `.env.sqlite` file and setting
# DATABASE_URL="file:./dev.db" before running tests or `npm run dev:sqlite`.
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=escrow_mvp_dev
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/escrow_mvp_dev

# Session signing secret (set to a long, random value in production)
SESSION_SECRET=change_this_to_a_secure_random_value

# Node environment
NODE_ENV=development

# Optional: local SQLite URL for quick local testing without Docker
# DATABASE_URL="file:./dev.db"
